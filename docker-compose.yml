services:
  postgres:
    image: postgres:17
    container_name: eventsituation-postgres
    environment:
      POSTGRES_DB: eventsituation
      POSTGRES_USER: eventsituation
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: